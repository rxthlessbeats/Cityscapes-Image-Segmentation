# Training hyperparameters (loaded by TrainConfig)
img_height: 256
img_width: 512
batch_size: 4
num_classes: 8
num_epochs: 30
lr: 0.0001
weight_decay: 0.001
num_train: 400
num_val: 100
seed: 42
loss_type: cross_entropy   # or "focal"
focal_gamma: 2.0
